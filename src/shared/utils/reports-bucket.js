/**
 * CloudFormation generated bucket names
 */
module.exports = function getReportsBucket () {
  // TODO (reports) need proper bucket names.
  throw new Error('TODO need proper keys')
  /*
  const isProduction = process.env.NODE_ENV === 'production'
  return isProduction
    ? 'liproduction-reportsbucket-TODO-GET-PROD-NAME'
    : 'listaging-reportsbucket-TODO-GET-STAGING-NAME'
  */
}
