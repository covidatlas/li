{
  "name": "li",
  "version": "0.0.0",
  "scripts": {
    "start": "arc sandbox",
    "lint": "eslint . --ignore-pattern node_modules --fix",
    "test": "npm run lint && npm run test:unit",
    "test:unit": "tape tests/unit/**/*-test.js | tap-spec"
  },
  "dependencies": {
    "// This is a serverless monorepo": "null",
    "// All root deps are considered dev dependencies": "null"
  },
  "devDependencies": {
    "@architect/architect": "^6.3.3",
    "eslint": "^6.8.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.1.3",
    "tap-spec": "^5.0.0",
    "tape": "^4.13.2"
  },
  "author": "Ryan Block <hello@ryanblock.com>",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=12.15.0 <13.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run lint"
    }
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix"
    ]
  }
}
